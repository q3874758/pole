[package]
name = "pole-rewards"
version = "0.1.0"
edition = "2021"

[dependencies]
pole-types = { path = "../../core/types" }
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
tokio-test = "0.4"
